{
  "name": "gamid-intro-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "node scripts/validate.mjs",
    "typecheck": "node --check dist/app.js && node --check dist/transition-engine.js",
    "test": "node --test",
    "build": "npm run lint && npm run typecheck && npm test"
  }
}
